# /etc/portage/env/no-optimize.env
# Para pacotes que quebram com -O2 — raro, mas acontece
# -O1 preserva a maioria das otimizações sem as agressivas

CFLAGS="-march=znver2 -O1 -pipe"
CXXFLAGS="${CFLAGS}"
